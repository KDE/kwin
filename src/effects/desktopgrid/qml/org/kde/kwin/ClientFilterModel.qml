/**
 * Dave, we could use the real one here?
 */
ClientModel
{
    property var activity
    property var desktop
    property string screenName
    property var clientModel
    property int windowType
}
